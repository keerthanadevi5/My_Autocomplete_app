import { ArrayDirective } from './array.directive';

describe('ArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayDirective();
    expect(directive).toBeTruthy();
  });
});
